{
  "version": 2,
  "buildCommand": "astro build",
  "outputDirectory": "dist",
  "framework": "astro",
  "installCommand": "npm install",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "astro build",
        "outputDirectory": "dist"
      }
    }
  ]
}
